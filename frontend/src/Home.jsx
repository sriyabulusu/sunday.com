import React from 'react';
import FakeStreaming from './FakeStreaming';

const Home = ({ formData, handleChange, handleSubmit }) => {
  return (
    <div>
      HOME


    </div>
  );
};

export default Home;